# SMTP settings (Gmail shown; adjust for other providers)
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=your-email@gmail.com
EMAIL_PASSWORD=your-gmail-app-password

# Email address that receives enquiries
RECIPIENT_EMAIL=jivaneklo@gmail.com

# Comma-separated list of allowed CORS origins (use * for all, or specify your domain)
ALLOWED_ORIGINS=https://locallens.homes

# Set to true during local development only
FLASK_DEBUG=false

# Port (Heroku and Replit set this automatically)
# PORT=5000
